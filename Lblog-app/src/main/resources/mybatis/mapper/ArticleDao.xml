<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lbwxxc.infrastructure.dao.ArticleDao">

    <resultMap id="BaseResultMap" type="top.lbwxxc.infrastructure.dao.po.Article">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="cover" jdbcType="VARCHAR" property="cover" />
        <result column="summary" jdbcType="VARCHAR" property="summary" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
        <result column="is_deleted" jdbcType="TINYINT" property="isDeleted" />
        <result column="read_num" jdbcType="INTEGER" property="readNum" />
    </resultMap>

    <sql id="Base_Column_List">
        id, title, cover, summary, create_time, update_time, is_deleted, read_num
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from article
        where id = #{id,jdbcType=BIGINT}
            and is_deleted = 0
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from article
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="top.lbwxxc.infrastructure.dao.po.Article" useGeneratedKeys="true">
        insert into article (title, cover, summary,
                             create_time, update_time, is_deleted,
                             read_num)
        values (#{title,jdbcType=VARCHAR}, #{cover,jdbcType=VARCHAR}, #{summary,jdbcType=VARCHAR},
                #{createTime,jdbcType=TIMESTAMP}, #{updateTime,jdbcType=TIMESTAMP}, #{isDeleted,jdbcType=TINYINT},
                #{readNum,jdbcType=INTEGER})
    </insert>

    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="top.lbwxxc.infrastructure.dao.po.Article" useGeneratedKeys="true">
        insert into article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">
                title,
            </if>
            <if test="cover != null">
                cover,
            </if>
            <if test="summary != null">
                summary,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
            <if test="updateTime != null">
                update_time,
            </if>
            <if test="isDeleted != null">
                is_deleted,
            </if>
            <if test="readNum != null">
                read_num,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">
                #{title,jdbcType=VARCHAR},
            </if>
            <if test="cover != null">
                #{cover,jdbcType=VARCHAR},
            </if>
            <if test="summary != null">
                #{summary,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null">
                #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updateTime != null">
                #{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="isDeleted != null">
                #{isDeleted,jdbcType=TINYINT},
            </if>
            <if test="readNum != null">
                #{readNum,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="top.lbwxxc.infrastructure.dao.po.Article">
        update article
        <set>
            <if test="title != null">
                title = #{title,jdbcType=VARCHAR},
            </if>
            <if test="cover != null">
                cover = #{cover,jdbcType=VARCHAR},
            </if>
            <if test="summary != null">
                summary = #{summary,jdbcType=VARCHAR},
            </if>
            <if test="createTime != null">
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted,jdbcType=TINYINT},
            </if>
            <if test="readNum != null">
                read_num = #{readNum,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="top.lbwxxc.infrastructure.dao.po.Article">
        update article
        set title = #{title,jdbcType=VARCHAR},
            cover = #{cover,jdbcType=VARCHAR},
            summary = #{summary,jdbcType=VARCHAR},
            update_time = #{updateTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="logicalDeleteByPrimaryKey" >
        update article
        set is_deleted = 1
        where id = #{id}
    </update>

    <select id="selectArticlePageList" resultMap="BaseResultMap">
        select *
        from article
        where is_deleted = 0
        <if test="startDate != null and endDate != null">
            and date(create_time) between #{startDate} and #{endDate}
        </if>
        <if test="title != null and title != ''">
            and title like concat('%', #{title}, '%')  <!-- 拼接%实现模糊匹配，仅当 title 非空时生效 -->
        </if>
        order by id desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="selectArticleSize">
        select count(1)
        from article
        where is_deleted = 0
    </select>

    <select id="selectArticleListByCategoryId" resultMap="BaseResultMap">
        SELECT a.*
            FROM article a
            INNER JOIN article_category_rel acr
        ON a.id = acr.article_id
            AND acr.is_deleted = 0
            AND acr.category_id = #{categoryId}
        WHERE
            a.is_deleted = 0
        GROUP BY a.id
        ORDER BY a.id desc
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectArticleListByTag" resultMap="BaseResultMap">
        SELECT a.*
        FROM article a
                INNER JOIN article_tag_rel atr
                            ON a.id = atr.article_id
                                AND atr.is_deleted = 0
                                AND atr.tag_id = #{tagId}
        WHERE
            a.is_deleted = 0
        GROUP BY a.id
        ORDER BY a.id desc
        LIMIT #{offset}, #{pageSize}
    </select>
</mapper>